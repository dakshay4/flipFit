
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;
import java.util.Locale;

public class DateUtil {

   /* public static LocalDate getDate(String date) {
        DateTimeFormatter f = DateTimeFormatter.ofPattern("dd-MM-yyyy", Locale.ENGLISH);
        LocalDate localDate = LocalDate.parse(date,f);
        return localDate;
    }

    public static String getDayOfWeek(String date) {
//        System.out.println(DateUtil.getDate("15-09-2023").getDayOfWeek().toString());
        return DateUtil.getDate(date).getDayOfWeek().toString();
    }*/
}
